"use client";

export function LoadOlderButton() {
  return (
    <button
      type="button"
      onClick={() => {}}
      className="rounded-full border border-brand bg-transparent px-8 py-2.5 text-sm font-semibold text-brand transition-colors hover:bg-accent-dim"
    >
      Load Older Messages
    </button>
  );
}